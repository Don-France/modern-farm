// Define an arrow function named createPotato to create an potato object and export the function.
// Return an object with the .type, .height and.output properties and values

export const createPotato = () => {
  const potatoSeed = { type: "Potato", height: 32, output: 2 };
  return potatoSeed;
};
