// Define an arrow function named createSoybean to create an soybean object and export the function.
// Return an object with the .type, .height and.output properties and values

export const createSoybean = () => {
  const soybeanObject = { type: "Soybean", height: 20, output: 4 };
  return soybeanObject;
};
