// Define an arrow function named createWheat to create an wheat object and export the function.
// Return an object with the .type, .height and.output properties and values

export const createWheat = () => {
  const wheatObject = { type: "Wheat", height: 230, output: 6 };
  return wheatObject;
};
